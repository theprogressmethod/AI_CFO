services:
  - type: web
    name: financial-processor
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node build
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
